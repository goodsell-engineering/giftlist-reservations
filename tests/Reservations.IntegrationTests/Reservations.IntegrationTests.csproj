<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="coverlet.collector" Version="6.0.4" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.14.1" />
    <PackageReference Include="xunit" Version="2.9.3" />
    <PackageReference Include="xunit.runner.visualstudio" Version="3.1.4" />
    <!-- CONVENTIONS.md "Testing": all infrastructure real via Testcontainers — never an in-memory
         substitute. GL-35's scope has no Rebus handler yet (GL-36), so this suite starts only
         Mongo, not RabbitMQ too — there is no messaging code here for a broker to exercise, and
         this machine has exactly one Testcontainers slot. Add Testcontainers.RabbitMq back here
         when GL-36 adds the ReserveGift handler. -->
    <PackageReference Include="Testcontainers.MongoDb" Version="4.14.0" />
    <PackageReference Include="Microsoft.Extensions.Hosting" Version="10.0.0" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Reservations.Host\Reservations.Host.csproj" />
    <!-- GL-93: ReliableReadiness's WithReliableWaitStrategy/StartReliablyAsync, required of every
         Testcontainers builder this suite starts (SuiteLabelRuleTests). -->
    <PackageReference Include="BuildingBlocks.Testing" Version="[0.1.0]" />
  </ItemGroup>
</Project>

